<div class="widget widget-recent-articles">
  <div class="widget-title">
    <h3><span>Letze Beiträge</span></h3>
  </div>
  <div class="widget-content">
    <ul>
      {% for article in blog.articles limit:10 %}
      <li class="article">
        <p class="blog_author">
          <span class="blog_author">{{'typo_blog.article.posted_in' | t }} 
            <time datetime="{{ article.published_at | date: '%Y-%m-%d' }}">
              {{ article.published_at | date: '%b %d, %Y' }}
            </time> {% if blog.comments_enabled? %}
            - <a href="{{ article.url }}#comments">
            {{ article.comments_count }} {{ article.comments_count | pluralize: 'comment','comments' }}
            </a>
            {% endif %} </span>  
        </p>
        <p class="recent-title"><a href="{{ article.url }}">{{ article.title }}</a></p>
      </li>
      {% endfor %}
    </ul>
  </div>
</div>
